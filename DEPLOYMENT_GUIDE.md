# GitHub Pages 部署指南

## 🚀 快速部署步骤

### 步骤1: 创建GitHub仓库
1. 登录 [GitHub.com](https://github.com)
2. 点击右上角 "+" → "New repository"
3. 填写仓库信息：
   - **Repository name**: `xiaoju` (或你喜欢的名字)
   - **Description**: 电车充电数据可视化面板
   - **Visibility**: Public (GitHub Pages需要公开仓库)
   - **Initialize with README**: ✅ 勾选
4. 点击 "Create repository"

### 步骤2: 本地Git初始化和推送

在终端中执行以下命令：

```bash
# 1. 进入项目目录
cd /Users/m4/Desktop/xiaoju

# 2. 初始化Git仓库
git init

# 3. 添加所有文件到暂存区
git add .

# 4. 提交更改
git commit -m "Initial commit: 电车充电数据可视化面板"

# 5. 关联远程仓库（将你的用户名和仓库名替换）
git branch -M main
git remote add origin https://github.com/你的用户名/xiaoju.git

# 6. 推送到GitHub
git push -u origin main
```

### 步骤3: 配置GitHub Pages

1. 在GitHub上进入你的仓库
2. 点击 **Settings** → **Pages**
3. 在 **Build and deployment** 部分：
   - **Source**: 选择 "Deploy from a branch"
   - **Branch**: 选择 `main` 和 `/docs` 文件夹
4. 点击 **Save**

### 步骤4: 等待部署

- GitHub需要1-2分钟来部署你的网站
- 部署成功后，页面顶部会显示绿色提示：
  ```
  ✅ Your site is live at https://你的用户名.github.io/xiaoju/
  ```

## 🎯 替代部署方案

### 方案A: 使用GitHub Desktop（图形界面）

1. 下载并安装 [GitHub Desktop](https://desktop.github.com/)
2. File → Add local repository → 选择 `/Users/m4/Desktop/xiaoju`
3. 提交所有更改 (Commit to main)
4. Publish repository → 选择GitHub账号 → 创建仓库
5. 在网页上按上述步骤配置Pages

### 方案B: 使用GitHub网页界面

1. 在GitHub创建新仓库
2. 点击 "Add file" → "Upload files"
3. 上传整个 `docs` 文件夹的内容
4. 进入Settings → Pages → 配置从main分支/docs目录部署

## 🔧 部署后的配置

### 自定义域名（可选）

如果你有自己的域名：

1. 在仓库根目录创建 `CNAME` 文件，内容为你的域名：
   ```
   your-domain.com
   ```

2. 在你的域名提供商处添加DNS记录：
   - 类型: `A`
   - 主机: `@`
   - 值: `185.199.108.153`
   - 值: `185.199.109.153`
   - 值: `185.199.110.153`
   - 值: `185.199.111.153`

3. 在GitHub Pages设置中填写你的自定义域名

### 更新数据

如果需要更新数据：

```bash
# 1. 修改 docs/data.json 或直接修改 index.html 中的数据
# 2. 提交更改
git add .
git commit -m "更新数据"
git push origin main
```

## 📊 部署状态检查

### 成功标志
- ✅ GitHub Pages设置页面显示绿色成功消息
- ✅ 访问网址能正常显示电车充电数据面板
- ✅ 所有图表和功能正常工作

### 常见问题解决

#### ❌ 部署失败
- 检查 `docs/index.html` 文件是否存在
- 确认分支设置正确（main分支，/docs目录）
- 查看GitHub Actions是否有错误信息

#### ❌ 页面404
- 等待几分钟，GitHub Pages需要时间部署
- 检查Settings → Pages中的配置是否正确
- 确认仓库是公开的（Private仓库需要GitHub Pro）

#### ❌ 数据不显示
- 检查浏览器控制台是否有错误
- 确认使用的是 `docs/index.html`（内嵌数据版本）
- 如果使用外部数据，确保 `data.json` 也在docs目录中

## 🎨 项目结构说明

```
xiaoju/                    # 项目根目录
├── docs/                  # GitHub Pages部署目录
│   ├── index.html        # 主页面（内嵌数据）
│   ├── data.json         # 数据文件（备用）
│   └── README.md         # 部署说明
├── .gitignore            # Git忽略文件
├── CNAME                 # 自定义域名配置（可选）
├── DEPLOYMENT_GUIDE.md   # 本部署指南
└── README.md             # 项目说明
```

## 📝 部署后的网址格式

- **标准格式**: `https://你的用户名.github.io/仓库名/`
- **示例**: `https://zhangsan.github.io/xiaoju/`

## 🆘 需要帮助？

如果在部署过程中遇到问题：

1. 检查 [GitHub Pages官方文档](https://docs.github.com/pages)
2. 查看GitHub仓库的 **Actions** 标签页查看部署日志
3. 在 **Settings** → **Pages** 查看部署状态

---

**祝部署成功！** 🚀